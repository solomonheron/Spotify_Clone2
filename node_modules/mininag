Kubernetes:

Go to PowerShell type:

# 1. Point Docker CLI to Minikube’s internal Docker
minikube docker-env | Invoke-Expression

# 2. Delete old Minikube (optional but recommended)
minikube delete

# 3. Start Minikube using Docker Desktop
minikube start --driver=docker

# 4. Create deployment named "mydocker"
kubectl create deployment mydocker --image=nginx:alpine

# 5. Scale deployment to 3 replicas
kubectl scale deployment mydocker --replicas=3

# 6. Expose deployment as NodePort service
kubectl expose deployment mydocker --port=80 --type=NodePort

# 7. Check the service
kubectl get svc

# 8. Open service in browser
minikube service mydocker


Nagios:
docker pull jasonrivers/nagios
docker run -d --name nagios -p 8080:80 jasonrivers/nagios

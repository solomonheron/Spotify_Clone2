Kubernetes:

Go to PowerShell type:

# 1. Point Docker CLI to Minikube's Docker
minikube docker-env | Invoke-Expression

# 2. Start Minikube using Docker driver
minikube start --driver=docker

# 3. Create deployment (nginx)
kubectl create deployment myapp --image=nginx:alpine

# 4. Scale deployment to 3 replicas
kubectl scale deployment myapp --replicas=3

# 5. Expose deployment as a NodePort service
kubectl expose deployment myapp --port=80 --type=NodePort

# 6. View services
kubectl get svc

# 7. Open service in browser
minikube service myapp


Nagios:
docker pull jasonrivers/nagios
docker run -d --name nagios -p 8080:80 jasonrivers/nagios
